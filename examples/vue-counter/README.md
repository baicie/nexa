# Vue Counter (Slice 6)

Vue 3 `createRenderer` → `@nexa/adapter-vue` → NUI Host.

```bash
pnpm install
perry compile main.ts -o vue-counter
./vue-counter
```

Uses Composition API + `h()` render functions (no SFC / DOM compiler).
