# @nexa/nui-host

Perry native library exposing the Nexa UI **Host Protocol**.

```ts
import { createNode, createText, insert, run, NodeType } from "@nexa/nui-host";
```

Build / validate:

```bash
cd packages/nui-host
cargo build --release
perry native validate
```
